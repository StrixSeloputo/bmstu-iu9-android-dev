<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textViewStyle">@style/CustomStyle.TextView</item>
    </style>
    <!-- Base application style for TextView -->
    <style name="CustomStyle.TextView" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:padding">16dp</item>
        <item name="android:textSize">20sp</item>
    </style>
    <!-- Base application style for Buttons -->
    <style name="CustomStyle.Button" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:padding">16dp</item>
        <item name="android:textSize">20sp</item>
    </style>

</resources>
